schema {
  query: Query
}

type Board {
  id: String
  url: String
  name: String
}

type Image {
  id: String
  url: String
  pinUrl: String
}

type Query {
  session: Session
}

type Session {
  loggedin: Boolean
  boards: [Board]
  images(boardId: String): [Image]
}
